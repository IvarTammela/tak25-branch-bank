services:
  branch-api:
    build: .
    command: npm start
    ports:
      - "8081:8081"
    env_file:
      - .env
    volumes:
      - ./data:/app/data

  bank-worker:
    build: .
    command: npm run start:worker
    env_file:
      - .env
    volumes:
      - ./data:/app/data
